import React, { useRef, useEffect } from 'react';
import { useInView, animate } from 'motion/react';

export const AnimatedNumber = ({ value }: { value: number }) => {
    const ref = useRef<HTMLSpanElement>(null);
    const isInView = useInView(ref, { once: true });

    useEffect(() => {
        if (!isInView) return;

        // ⚡ Bolt: Direct DOM manipulation with Framer Motion `animate`
        // Why: Prevents numerous React re-renders during animation (previously caused by useState)
        // Impact: Bypasses the render cycle entirely while preserving consistent animation speed
        const controls = animate(0, value, {
            duration: 2, // Framer Motion v12 expects seconds
            ease: "linear",
            onUpdate(currentValue) {
                if (ref.current) {
                    ref.current.textContent = Math.floor(currentValue).toString();
                }
            },
            onComplete() {
                if (ref.current) {
                    ref.current.textContent = value.toString();
                }
            }
        });

        return () => controls.stop();
    }, [isInView, value]);

    return <span ref={ref}>0</span>;
};
