## 2026-08-05 - Framer Motion v12 Animate Duration Parameter

**Learning:** When using Framer Motion v12's `animate` function, the `duration` parameter must be specified in seconds (e.g., `duration: 2`), not milliseconds.

**Action:** Always verify the expected units for duration parameters when working with animation libraries to prevent unexpected behavior.
